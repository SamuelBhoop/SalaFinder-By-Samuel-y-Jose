# En desarrollo el proxy de Vite reenvía /api → backend (ver vite.config.js)
# Solo cambia VITE_BACKEND_URL si el backend corre en otro puerto
VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:5155
